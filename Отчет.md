## EDA-анализ датасета Titanic
__*EDA анализ*__ (разведочный анализ) – первичный анализ данных, целями которого являются понимание структуры данных, поиск зависимостей и закономерностей, обнаружение пропусков и выбросов, а также подготовка данных к моделированию.

Анализ будет проводиться на примере **датасета Titanic** ([train.csv](https://www.kaggle.com/competitions/titanic/data?select=train.csv)), содержащего данные о каждом пассажире Титаника с пометкой, выжил ли он или нет.

Для работы с датасетом будут использоваться: 
* __Kaggle Notebook__ (для запуска кода) ;
* __Python__ (для написания кода); 
* __pandas__ (библиотека для работы с датасетами, данными и .csv файлами);
* __matplotlib__ и __seaborn__ (библиотеки для отрисовки графиков).

### Первичный обзор структуры датафрейма

Создадим датафрейм из .csv файла:
```
test_data = pd.read_csv("/kaggle/input/competitions/titanic/test.csv")
```

Для получения информации о структуре датафрейма, его столбцах, пропусках, использованной памяти, типах данных и их количестве воспользуемся `info()` и `describe()`.

Датафрейм насчитывает **12** колонок и **891** запись. Структура колонок приведена ниже.

```
<class 'pandas.core.frame.DataFrame'>
RangeIndex: 891 entries, 0 to 890
Data columns (total 12 columns):
 #   Column       Non-Null Count  Dtype  
---  ------       --------------  -----  
 0   PassengerId  891 non-null    int64  
 1   Survived     891 non-null    int64  
 2   Pclass       891 non-null    int64  
 3   Name         891 non-null    object 
 4   Sex          891 non-null    object 
 5   Age          714 non-null    float64
 6   SibSp        891 non-null    int64  
 7   Parch        891 non-null    int64  
 8   Ticket       891 non-null    object 
 9   Fare         891 non-null    float64
 10  Cabin        204 non-null    object 
 11  Embarked     889 non-null    object 
dtypes: float64(2), int64(5), object(5)
memory usage: 83.7+ KB
```
Рассмотрим каждую колонку поподробнее:
* __*PassengerId*__ (тип данных *int64*, пропусков нет) - id пассажира;
* __*Survived*__ (тип данных *int64*, пропусков нет) - признак того, выжил ли пассажир, где __*1 - выжил*__ / __*0 - не выжил*__;
* __*Pclass*__ (тип данных *int64*, пропусков нет) - класс посадочного билета (__*1-й*__, __*2-й*__, __*3-й*__);
* __*Name*__ (тип данных *object*, пропусков нет) - имя пассажира;
* __*Sex*__ (тип данных *object*, пропусков нет) - пол пассажира;
* __*Age*__ (тип данных *float64*, пропуски **есть**) - возраст пассажира в годах;
* __*SibSp*__ (тип данных *int64*, пропусков нет) - количество братьев и сестер / супругов;
* __*Parch*__ (тип данных *int64*, пропусков нет) - количество родителей / детей;
* __*Ticket*__ (тип данных *object*, пропусков нет) - номер билета;
* __*Fare*__ (тип данных *float64*, пропусков нет) - стоимость проезда;
* __*Cabin*__ (тип данных *object*, есть пропуски **есть**) - номер каюты;
* __*Embarked*__ (тип данных *object*, пропуски **есть**) - порт отправления, где __*C - Cherbourg*__, __*Q - Queenstown*__, __*S - Southampton*__.

> Итого: **2** типа данных __*float64*__, **5** типов данных __*int64*__, **5** типов данных __*object*__

### Поиск и обработка пропусков
Для поиска и подсчета количества пропусков воспользуемся ``isnull()`` и построим новый датафрейм:

```
print("\nПропущенные значения:")
missing = train_data.isnull().sum()
missing_pct = (missing / len(train_data) * 100).round(1)
missing_df = pd.DataFrame({"Количество": missing, "Процент (%)": missing_pct})
print(missing_df[missing_df["Количество"] > 0])
```
>Итоговое количество пропусков:
> * Age - **177** (*19.9%*);
> * Cabin - **687** (*77.1%*);
> * Embark - **2** (*0.2%*).

Возможные пути решения:
1. *Удаление колонки, если >50% пропусков*
2. *Подстановка на место пропуском медианных значений, средних значений или моды*

Таким образом: 
* колонку *Age* дозаполним медианными значениями с помощью **``median()``** : ``train_data["Age"].fillna(train_data["Age"].median())``;
* колонку *Cabin* не будем учитывать при анализе;
* колонку *Embarked* дозаполним модой с помощью **``mode()``** : ``train_data["Embark"].fillna(train_data["Embark"].mode())``.

### Анализ распределения числовых и категориальных признаков

Рассмотрим распределения количества пассажиров по полу, возрасту и стоимости билета. Построим столбчатую диаграмму для распределения количества пассажиров по полу (__*Sex*__) и гистограммы для распределений по возрасту (__*Age*__) и цене билета (__*Fare*__).
![plot1](/Task1/images/plot1.svg)
![plot2-3](/Task1/images/plot2-3.svg)
![plot4-5](/Task1/images/plot4-5.svg)

Из графиков  следует:
* пассажиров мужского пола было _**почти в 2 раза больше**_, чем пассажиров женского пола;
* преобладающая возрастная группа пассажиров - ***20-40 лет***, но также на корабле присутствовало большое количество детей;
* число пассажиров ***обратно пропорционально*** стоимости их билетов;
* среди возрастов и цен за билет присутствуют явно видимые выбросы: люди ***от 0 до 3 лет*** и ***старше 55*** и люди с билетом стоимостью ***больше 75 £***.

Также рассмотрим распределение количества пассажиров по количеству их братьев / сестер / супругов (***SibSp***) и по количеству их родителей / детей (***Parch***).

![plot6](/Task1/images/plot6.svg)
![plot7](/Task1/images/plot7.svg)

Из графиков следует:
* большая часть пассажиров ***были одиноки***;
* значительная часть путешествовала с ***1 братом / сестрой / супругом*** или ***семьей с 1-2 детьми***.

Также рассмотрим распределение количества пассажиров по классу билета (***Pclass***), а также средний возраст пассажира каждого класса.

![plot8-9](/Task1/images/plot8-9.svg)

Из графиков следует:
* ***большая*** часть пассажиров ехала ***3 классом***, ***меньше всего*** пассажиров - ***2 классом***;
* ***самые молодые*** пассажиры ехали ***3 классом***, ***самые возрастные*** - ***1 классом***.

На диаграмме ниже представлены доли выживших и погибших пассажиров.

![plot10](/Task1/images/plot10.svg)

Как видно из диаграммы, ***большинство*** людей ***погибли***.

Далее рассмотрим **выживаемость** по таким категориальным признакам, как пол (***Sex***), класс билета (***Pclass***), количество братьев / сестер / супругов (***SibSp***) и количество родителей / детей (***Parch***).

![plot11-14](/Task1/images/plot11-14.svg)

Из графиков следует:
* большая часть выживших - ***женщины***;
* класс билета влияет на выживаемость: чем ***выше класс***, тем ***больше выживаемость***;;
* больше половины выживших имели ***1-го*** брата / сестру / супруга (супругу);
* больше половины выживших имели ***хотя бы 1-го родителя*** или ***1-3 детей***.

Рассмотрим распределение возраста по выживаемости (***Age***).

![plot15](/Task1/images/plot15.svg)

Из графика следует:
* среди почти всех возрастов ***больше погибших***, чем выживших, ***кроме*** детей ***до 5 лет***, детей ***12-13 лет*** и людей ***48-51 год***.

Создадим новые признаки:
* ***FamilySize*** (размер семьи): ***SibSp*** (количество братьев / сестер / супругов) + ***Parch*** (количество родителей / детей) + 1;
* ***IsAlone*** (одинок ли пассажир (FamilySize = 1)): ***0 - есть семья***, ***1 - одинок***;
* ***AgeGroup*** (возрастная группа): 
  1. ***младенцы (0-1 год)*** 
  2. ***малыши (1-3 года)***
  3. ***дети (4-12 лет)*** 
  4. ***подростки (13-17 лет)*** 
  5. ***молодежь (18-34 года)***
  6. ***взрослые (35-60 лет)***
  7. ***возрастные (60+)***;
* ***Title*** (титул / статус в обществе):
  1. Мистер ***(Mr)***
  2. Миссис ***(Mrs)***
  3. Мисс ***(Miss)***
  4. Юноша ***(Master)***
  5. Дон ***(Don)***
  6. Преподобный ***(Rev)***
  7. Доктор ***(Dr)***
  8. Мадам ***(Mme)***
  9. Женщина (без СП) ***(Ms)*** 
  10. Майор ***(Major)***
  11. Леди ***(Lady)***
  12. Сир ***(Sir)***;
  13. Мадмуазэль ***(Mlle)***
  14. Полковник ***(Col)***
  15. Капитан ***(Capt)***
  16. Графиня ***(the Countess)***
  17. Йонкер ***(Jonkheer)***.
```
train_data["FamilySize"] = train_data["SibSp"] + train_data["Parch"] + 1

train_data["IsAlone"] = (train_data["FamilySize"] == 1).astype(int)

bins = [0, 1, 3, 12, 17, 34, 60, 120]
labels = ["0-1 (Младенцы)", "1-3 (Малыши)", "4-12 (Дети)", "13-17 (Подростки)", 
          "18-34 (Молодежь)", "35-60 (Взрослые)", "60+ (Возрастные)"]

train_data["AgeGroup"] = pd.cut(train_data["Age"], bins=bins, labels=labels)
train_data["Title"] = train_data["Name"].str.extract(r',\s*([^\.]+)\.')
```
Рассмотрим распределение количества пассажиров по новым признакам.

![plot16-19](/Task1/images/plot16-19.svg)

Из графиков следует:
* большая часть пассажиров - ***одинокие молодые люди*** или ***взрослые***, ***без редких титулов*** в обществе.

Рассмотрим распределение выживаемости по новым признакам.

![plot20-23](/Task1/images/plot20-23.svg)

Из графиков следует:
* больше всего выжило людей с размером семьи от ***2*** до ***4*** человек;
* больше всего выжило ***младенцев***, ***малышей*** и ***детей***;
* замужних женщин выжило больше, чем незамужних;
* среди распространенных титулов больше всего выжило ***миссис***, ***мисс*** и ***юношей***;
* все ***женщины с высоким статусом*** в обществе (редкий женский титул) ***выжили***, ***мужчин с редкими, но рабочими титулами*** (*доктор*, *полковник*, *майор*) ***выжила половина или меньше*** (исключение составляет *капитан*), ***мужчины*** с ***редкими почетными титулами*** (*сир*) выжили, а с ***непочетными титулами*** (*йонкер*) ***не выжили***.

Построим корреляционную матрицу.

![plot24](/Task1/images/plot24.svg)

Выводы из корреляционной матрицы приведем в общем к работе выводе.

### Вывод
Самые важные признаки, влияющие на выживаемость - ***пол***, ***класс билета и стоимость билета*** (чем выше класс, тем выше стоимость), ***одинок ли пассажир*** или ***нет***. *Возраст*, *размер семьи* и *титул* имеют более слабую корреляцию. 
#### Выживаемость
* чаще всего - ***женщины*** и ***дети***;
* ***выше стоимость*** билета (выше класс) - ***выше процент выживаемости***;
* чаще всего выживали ***люди с семьей***, причем ***небольшого размера***;
* все ***женщины с редкими титулами*** выжили.
#### Смертность
* чаще всего - ***мужчины среднего возраста с 3 классом билета*** (низкая стоимость);
* ***одинокие*** люди ***умирали чаще***;
* даже ***редкие титулы*** по типу *капитана*, *доктора*, *дона* или *преподобного* ***не обеспечивали высокие шансы на выживаемость***.

Выявленные корреляции и закономерности соответствуют принятым в обществе моральным и этическим принципам и правилам (спасать женщин и детей, приоритет у богатых), что свидетельствует о корректности и правдоподобности полученных результатов.

# Решение регрессионной задачи предсказания стоимости дома

В данной работе рассматривается задача регрессии - предсказание продажной цены дома по его характеристикам с использованием набора данных [House Prices](https://www.kaggle.com/competitions/house-prices-advanced-regression-techniques) c платформы Kaggle. В качестве целевой переменной выступает числовая величина ***SalePrice***, а в качестве признаков используются как числовые (площадь, количество комнат, год постройки), так и категориальные переменные (тип районa, материал стен и т.д.). Для первичного анализа зависимости цены от признаков применяется линейная регрессия, а затем более сложные ансамблевые методы, в частности, ***алгоритм случайного леса***.

## Аналитическая часть
### Деревья решений

**Дерево решений** представляет собой модель, которая последовательно разбивает пространство признаков на области с помощью пороговых условий. Каждая внутренняя вершина дерева соответствует проверке условия вида ($x_j \leq t$), а каждый лист - итоговому предсказанию.

![img1](/Task2/images/img1.png)

Дерево решений для задачи регрессии строится путём рекурсивного разбиения обучающей выборки на непересекающиеся подмножества. На каждом шаге перебираются все признаки $x_j$ и все возможные пороги $t$; выбирается пара ($x_j^∗$, $t^∗$), ***минимизирующая взвешенную сумму *MSE* дочерних узлов***:

$$L(j,t)=\frac{|R_L|}{n}⋅MSE_L+\frac{∣R_R∣}{n}⋅MSE_R→min⁡$$ 
где $R_L = \{i : x_{ij} \leq t\}$ и $R_R = \{i : x_{ij} > t\}$ - множества номеров объектов, попадающих соответственно в левый и правый дочерние узлы при разбиении по признаку $j$ и порогу $t$,
$i$ - номер объекта в выборке,
$j$ - номер признака (поэтому $x_{ij}$ обозначает значение $j$‑го признака у $i$‑го объекта). 

После выбора оптимального разбиения (признака $x_j^∗$​ и порога $t^∗$ процесс повторяется рекурсивно для левого и правого дочерних узлов, пока не будут достигнуты заданные гиперпараметрами ограничения (максимальная глубина, минимальное число объектов в листе).

В задаче регрессии качество модели на отдельном множестве объектов оценивается с помощью **среднеквадратичной ошибки** **(MSE)** и её производной — **среднеквадратичного отклонения (RMSE)**. Пусть для $N$ объектов моделирование даёт предсказания $\hat{y}_1,…,\hat{y}_N$​, а истинные значения составляют $y_1,…,y_N$. Тогда:

$$MSE = \frac{1}{N}\sum_{i=1}^N(y_i - \hat{y}_i)^2, \ \ \ \ \ \ \ RMSE=\sqrt{MSE}.$$
RMSE имеет ту же размерность, что и целевая переменная (например, цены домов в тысячах долларов), поэтому его удобно интерпретировать как **среднее отклонение предсказаний модели от истинных значений**. В данной работе RMSE используется как **основная метрика качества** модели случайного леса; её значение рассчитывается как на обучающей выборке, так и на отдельном **тестовом множестве**.

Несмотря на простоту и интерпретируемость, деревья решений обладают существенным недостатком - **высокой чувствительностью к обучающей выборке**. Небольшие изменения в данных могут приводить к значительным изменениям структуры дерева и его предсказаний.

### Ансамбли

Основным недостатком деревьев решений является их склонность к переобучению и тот факт, что даже при небольшом изменении обучающей выборки дерево может значительно измениться. Однако их объединение в *ансамбли* или *композиции* на практике дает очень хорошие результаты. **Ансамбли** - это методы, сочетающие в себе несколько алгоритмов машинного обучения с целью получения более мощной модели.

В случае задачи регрессии при использовании композиции $\hat{y}(x)$ из N базовых алгоритмов T ответом будет считаться среднее значение ответа каждого алгоритма:
$$\hat{y}(x)=\frac{1}{N}\sum_{i=1}^nT_i(x),$$
где $\hat{y}(x)$ - итоговое предсказание случайного леса для объекта $x$,
$T_i$ - предсказание $i$-го дерева  для объекта $x$,
$N$ - число деревьев в лесу.

В задачах классификации, соответственно, знак полученного усредненного ответа или (что аналогично) класс определяется путем голосования: объект относится к классу, за который "проголосовало" наибольшее число базовых алгоритмов.

### Разложение ошибки на смещение и разброс

Ошибка алгоритмов складывается из смещения (bias) (отклонения среднего ответа обученного алгоритма от ответа идеального алгоритма) и разброса или дисперсии (variance) (разброс ответов обученных алгоритмов относительно среднего ответа). Также к этому разложению обычно прибавляется шум, который характеризует ошибку идеального алгоритма и которым никак не льзя управлять - это характеристика входных данных. Как правило, простые семейства алгоритмов (например, линейные классификаторы) характеризуются высоким смещением и низким разбросом, а сложные семейства (в т. ч. деревья) наоборот - низким смещением и высоким разбросом. Можно сказать так, что *разброс характеризует чувствительность метода обучения к выборке*, то есть насколько будет изменяться ответ обученного алгоритма в зависимости от изменений в обучающей выборке. 

$$
Ошибка\ на\ новых\ данных = Шум + Смещение + Разброс,
$$
где Шум - ошибка лучшей модели $\hat{y}(x)$,
Смещение - отклонение усредненных ответов моделей от ответов лучшей модели $\hat{y}(x)$,
Разброс - дисперсия ответов моделей.

Рассмотрим пример моделей с *низким* смещением и *большим* разбросом (*дерево решений*) и наоборот (*линейная регрессия*). Для этого обучим деревья решений на 10 выборках на примере функции $y = 6 - 13x -5x^2 -4x^3$ c добавлением шума.

![plot1](/Task2/images/plot1.svg)
#### Деревья решений
Ниже приведен результат обучения моделей деревьев решений.
![plot2](/Task2/images/plot2.svg)

Как видно из графика, модель обладает низким смещением (так как значения близки к истинным) и высоким разбросом (ответы разных моделей заметно отличаются друг от друга), что говорит о том, что модель нестабильна к изменениям выборки.

При усреднении предсказаний получается закон, близкий к истине (с низким смещением). 
![plot3](/Task2/images/plot3.svg)

Таким образом, модель дерева решений способна восстановить сложные зависимости.
#### Линейная регрессия
Ниже приведен результат обучения моделей линейной регрессии.

![plot4](/Task2/images/plot4.svg)

Как видно из графика, модель обладает высоким смещением (так как значения далеки от истины) и низким разбросом (ответы разных моделей близки друг к другу), что говорит о стабильности модели при изменении выборки.

При усреднении предсказаний получается закон, далекий от истины (с высоким смещением). 

![plot5](/Task2/images/plot5.svg)

Таким образом, модель линейной регрессии не способна восстановить сложные зависимости.

При усреднении алгоритмов:
* **не меняется** смещение;
* разброс = $\frac{разброс\ базового\ алгоритма}{N} + корреляция\ между\ базовыми\ алгоритмами$. 

Есть два способа, позволяющих уменьшить корреляцию:
* ***бэггинг*** (ансамблевый метод обучения базовых алгоритмов на случайной подвыборке, полученной бутстрап-методом);
![img3](/Task2/images/img3.svg)
* метод случайных подпространств (обучения базовых алгоритмов на случайном подмножестве признаков);
![img4](/Task2/images/img4.svg)
* комбинация двух методов выше.
### Бутстрап

Для построения ансамблевых алгоритмов используется метод **бутстрапа**: из исходной обучающей выборки случайным образом с возвращением формируются несколько подвыборок равного объёма. Каждая подвыборка используется для обучения отдельной базовой модели (дерева решений), а отсутствующие во вновь сгенерированной выборке объекты образуют ***out‑of‑bag*** ***подвыборку (OOB)***, которая может применяться для внутренней оценки качества модели без использования отдельного валидационного множества.

 Поскольку бутстрап выбирает объекты с возвращением, каждый объект имеет ненулевую вероятность не попасть в конкретную подвыборку. В среднем около **37%** всех объектов исходной выборки оказываются OOB‑объектами для одного конкретного дерева (точное значение связано с пределом $1-\frac{1}{e}≈0.6321$, поэтому доля попавших примерно 63%, а не попавших - около 37%).

Для оценки качества всего леса OOB‑механизм работает следующим образом: для каждого объекта исходной выборки собираются предсказания только тех деревьев, которые **не обучались** на этом объекте (то есть объект был OOB‑для этих деревьев); по ним вычисляется локальная ошибка, а затем усреднённая ошибка по всем объектам даёт **OOB‑оценку качества модели**. Это позволяет контролировать переобучение и подбирать гиперпараметры без необходимости выделять отдельное валидационное или тестовое множество.

В данной работе OOB‑оценка используется как **внутренняя валидация** для модели случайного леса, а финальная оценка качества осуществляется на отдельном **тестовом множестве** с помощью RMSE.

**Гиперпараметры** - это настройки модели, задаваемые до начала обучения, например, максимальная глубина деревьев, число признаков, рассматриваемых в каждом узле, и общее число деревьев в лесу. Они определяют архитектуру и сложность модели, но не обучаются из данных, поэтому их подбирают по валидационной или OOB‑оценке качества.

Для случайного леса типичные гиперпараметры:
* максимальная глубина дерева;
* минимальное число объектов в листе;
* количество деревьев в лесу;
* число признаков, рассматриваемых в каждом узле.

Валидационное множество используется для подбора гиперпараметров, а тестовое множество - для финальной проверки модели на независимой выборке. В данном случае OOB‑ошибка заменяет валидационное множество: по ней осуществляется подбор гиперпараметров и контроль склонности к переобучению без необходимости выделять отдельное валидационное или тестовое подмножество.

![img2](/Task2/images/img2.jpg)

## Случайный лес

**Случайный лес** (***Random Forest***) - ансамблевый метод машинного обучения, основанный на использовании множества **деревьев решений**.

При построении случайного леса вначале генерируется количество бутстрап-выборок, равное количеству деревьев в алгоритме. Для уменьшения корреляции базовых алгоритмов рандомизируют сам процесс построения каждого дерева: если в стандартном методе построения деревьев мы в каждом узле выбираем $j$-й признак и порог $t$, с которым сравнивается его значение, и потом эти значения оптимизируются с помощью функции ошибки, то в методе случайного леса в каждой вершине $j$-й признак выбирается не из всего пространства признаков, а из его случайного подмножества размера $m$, которое **каждый раз выбирается заново** (в этом и отличие от метода случайных подпространств, где пространство выбирается единожды и используется для построения всего дерева).

При построении случайных лесов в задаче регрессии рекомендуется брать $m=d/3$, где $d$ - общее число признаков, и строить дерево, пока на листьях не останется по пять объектов.

## Практическая часть

Для решения задачи будем обучать модель на примере **датасета House Pricing** ([train.csv](https://www.kaggle.com/competitions/house-prices-advanced-regression-techniques/data?select=train.csv)), содержащего подробные данные о каждом доме. Структура данных будет рассмотрена далее.

Для работы с датасетом будут использоваться: 
* __Kaggle Notebook__ (для запуска кода);
* __Python__ (для написания кода);
* __pandas__ (библиотека для работы с датасетами, данными и .csv файлами);
* __numpy__ (библиотека для работы с инструментами линейной алгебры);
* __TensorFlow__ (библиотека для машинного обучения);
* __ТensorFlow Decision Forests__ или ***TF-DF*** (библиотека для использования алгоритмов деревьев решений);
* __matplotlib__ и __seaborn__ (библиотеки для отрисовки графиков).

### Первичный анализ датасета

Создадим датафрейм из .csv файла:
```
dataset_df = pd.read_csv("/kaggle/input/competitions/house-prices-advanced-regression-techniques/train.csv")
```

Для получения информации о структуре датафрейма, его столбцах, пропусках, использованной памяти, типах данных и их количестве воспользуемся `info()` и `describe()`.

Датафрейм насчитывает **80** колонок и **1460** записей. Структура колонок приведена ниже.

```
<class 'pandas.core.frame.DataFrame'>
RangeIndex: 1460 entries, 0 to 1459
Data columns (total 80 columns):
 #   Column         Non-Null Count  Dtype  
---  ------         --------------  -----  
 0   MSSubClass     1460 non-null   int64  
 1   MSZoning       1460 non-null   object 
 2   LotFrontage    1201 non-null   float64
 3   LotArea        1460 non-null   int64  
 4   Street         1460 non-null   object 
 5   Alley          91 non-null     object 
 6   LotShape       1460 non-null   object 
 7   LandContour    1460 non-null   object 
 8   Utilities      1460 non-null   object 
 9   LotConfig      1460 non-null   object 
 10  LandSlope      1460 non-null   object 
 11  Neighborhood   1460 non-null   object 
 12  Condition1     1460 non-null   object 
 13  Condition2     1460 non-null   object 
 14  BldgType       1460 non-null   object 
 15  HouseStyle     1460 non-null   object 
 16  OverallQual    1460 non-null   int64  
 17  OverallCond    1460 non-null   int64  
 18  YearBuilt      1460 non-null   int64  
 19  YearRemodAdd   1460 non-null   int64  
 20  RoofStyle      1460 non-null   object 
 21  RoofMatl       1460 non-null   object 
 22  Exterior1st    1460 non-null   object 
 23  Exterior2nd    1460 non-null   object 
 24  MasVnrType     588 non-null    object 
 25  MasVnrArea     1452 non-null   float64
 26  ExterQual      1460 non-null   object 
 27  ExterCond      1460 non-null   object 
 28  Foundation     1460 non-null   object 
 29  BsmtQual       1423 non-null   object 
 30  BsmtCond       1423 non-null   object 
 31  BsmtExposure   1422 non-null   object 
 32  BsmtFinType1   1423 non-null   object 
 33  BsmtFinSF1     1460 non-null   int64  
 34  BsmtFinType2   1422 non-null   object 
 35  BsmtFinSF2     1460 non-null   int64  
 36  BsmtUnfSF      1460 non-null   int64  
 37  TotalBsmtSF    1460 non-null   int64  
 38  Heating        1460 non-null   object 
 39  HeatingQC      1460 non-null   object 
 40  CentralAir     1460 non-null   object 
 41  Electrical     1459 non-null   object 
 42  1stFlrSF       1460 non-null   int64  
 43  2ndFlrSF       1460 non-null   int64  
 44  LowQualFinSF   1460 non-null   int64  
 45  GrLivArea      1460 non-null   int64  
 46  BsmtFullBath   1460 non-null   int64  
 47  BsmtHalfBath   1460 non-null   int64  
 48  FullBath       1460 non-null   int64  
 49  HalfBath       1460 non-null   int64  
 50  BedroomAbvGr   1460 non-null   int64  
 51  KitchenAbvGr   1460 non-null   int64  
 52  KitchenQual    1460 non-null   object 
 53  TotRmsAbvGrd   1460 non-null   int64  
 54  Functional     1460 non-null   object 
 55  Fireplaces     1460 non-null   int64  
 56  FireplaceQu    770 non-null    object 
 57  GarageType     1379 non-null   object 
 58  GarageYrBlt    1379 non-null   float64
 59  GarageFinish   1379 non-null   object 
 60  GarageCars     1460 non-null   int64  
 61  GarageArea     1460 non-null   int64  
 62  GarageQual     1379 non-null   object 
 63  GarageCond     1379 non-null   object 
 64  PavedDrive     1460 non-null   object 
 65  WoodDeckSF     1460 non-null   int64  
 66  OpenPorchSF    1460 non-null   int64  
 67  EnclosedPorch  1460 non-null   int64  
 68  3SsnPorch      1460 non-null   int64  
 69  ScreenPorch    1460 non-null   int64  
 70  PoolArea       1460 non-null   int64  
 71  PoolQC         7 non-null      object 
 72  Fence          281 non-null    object 
 73  MiscFeature    54 non-null     object 
 74  MiscVal        1460 non-null   int64  
 75  MoSold         1460 non-null   int64  
 76  YrSold         1460 non-null   int64  
 77  SaleType       1460 non-null   object 
 78  SaleCondition  1460 non-null   object 
 79  SalePrice      1460 non-null   int64  
dtypes: float64(3), int64(34), object(43)
memory usage: 912.6+ KB
```
Рассмотрим каждую колонку поподробнее:
* ***MSSubClass*** (тип данных *int64*, пропусков нет) - класс жилого помещения;
* ***MSZoning*** (тип данных *object*, пропусков нет) - категория земли;
* ***LotFrontage*** (тип данных *float64*, **259** пропусков) - длина улицы, прилегающей к участку;
* ***LotArea*** (тип данных *int64*, пропусков нет) - размер участка;
* ***Street*** (тип данных *object*, пропусков нет) - тип подъездной дороги;
* ***Alley*** (тип данных *object*, **1369** пропусков) - тип доступа через переулок;
* ***LotShape*** (тип данных *object*, пропусков нет) - общая форма земельного участка;
* ***LandContour*** (тип данных *object*, пропусков нет) - контур участка;
* ***Utilities*** (тип данных *object*, пропусков нет) - виды доступных коммунальных услуг;
* ***LotConfig*** (тип данных *object*, пропусков нет) - конфигурация лота;
* ***LandSlope*** (тип данных *object*, пропусков нет) - уклон территории;
* ***Neighborhood*** (тип данных *object*, пропусков нет) - район городской черты Эймса;
* ***Condition1*** (тип данных *object*, пропусков нет) - близость к главной дороге или железной дороге;
* ***Condition2*** (тип данных *object*, пропусков нет) - близость к главной дороге или железной дороге (если еще одна таковая имеется);
* ***BldgType*** (тип данных *object*, пропусков нет) - тип жилого помещения;
* ***HouseStyle*** (тип данных *object*, пропусков нет) - стиль жилища;
* ***OverallQual*** (тип данных *int64*, пропусков нет) - общее качество материалов и отделки;
* ***OverallCond*** (тип данных *int64*, пропусков нет) - общая оценка состояния;
* ***YearBuilt*** (тип данных *int64*, пропусков нет) - дата строительства;
* ***YearRemodAdd*** (тип данных *int64*, пропусков нет) - дата реконструкции;
* ***RoofStyle*** (тип данных *object*, пропусков нет) - тип кровли;
* ***RoofMatl*** (тип данных *object*, пропусков нет) - кровельный материал;
* ***Exterior1st*** (тип данных *object*, пропусков нет) - внешнее покрытие дома;
* ***Exterior2nd*** (тип данных *object*, пропусков нет) - внешнее покрытие дома (если используется более одного материала);
* ***MasVnrType*** (тип данных *object*, **872** пропуска) - тип облицовки из кирпичной кладки;
* ***MasVnrArea*** (тип данных *float64*, **8** пропусков) - площадь облицовки из кирпича в квадратных футах;
* ***ExterQual*** (тип данных *object*, пропусков нет) - качество наружных материалов;
* ***ExterCond*** (тип данных *object*, пропусков нет) - текущее состояние материала на внешней поверхности;
* ***Foundation*** (тип данных *object*, пропусков нет) - тип фундамента;
* ***BsmtQual*** (тип данных *object*, **37** пропусков) - высота подвала;
* ***BsmtCond*** (тип данных *object*, **37** пропусков) - общее состояние подвала;
* ***BsmtExposure*** (тип данных *object*, **38** пропусков) - стены подвала с выходом на улицу или на уровне сада;
* ***BsmtFinType1*** (тип данных *object*, **37** пропусков) - качество отделки подвального помещения;
* ***BsmtFinSF1*** (тип данных *int64*, пропусков нет) - тип 1, готовая площадь в квадратных футах;
* ***BsmtFinType2*** (тип данных *object*, **38** пропусков) - качество второй обработанной зоны (если таковая имеется);
* ***BsmtFinSF2*** (тип данных *int64*, пропусков нет) - тип 2, готовая площадь в квадратных футах;
* ***BsmtUnfSF*** (тип данных *int64*, пропусков нет) - недостроенная площадь подвального помещения в квадратных футах;
* ***TotalBsmtSF*** (тип данных *int64*, пропусков нет) - общая площадь подвального помещения в квадратных футах;
* ***Heating*** (тип данных *object*, пропусков нет) - тип отопления;
* ***HeatingQC*** (тип данных *object*, пропусков нет) - качество и состояние отопления;
* ***CentralAir*** (тип данных *object*, пропусков нет) - центральное кондиционирование воздуха;
* ***Electrical*** (тип данных *object*, **1** пропуск) - электрическая система;
* ***1stFlrSF*** (тип данных *int64*, пропусков нет) - площадь первого этажа в квадратных футах;
* ***2ndFlrSF*** (тип данных *int64*, пропусков нет) - площадь первого этажа в квадратных футах;
* ***LowQualFinSF*** (тип данных *int64*, пропусков нет) - низкое качество отделки (все этажи);
* ***GrLivArea*** (тип данных *int64*, пропусков нет) -  площадь жилого пространства над уровнем земли (в квадратных футах);
* ***BsmtFullBath*** (тип данных *int64*, пропусков нет) - полностью оборудованные ванные комнаты в подвале;
* ***BsmtHalfBath*** (тип данных *int64*, пропусков нет) - туалеты в подвале;
* ***FullBath*** (тип данных *int64*, пропусков нет) - полностью оборудованные ванные комнаты расположены над уровнем земли;
* ***HalfBath*** (тип данных *int64*, пропусков нет) - туалеты расположены над уровнем земли;
* ***BedroomAbvGr*** (тип данных *int64*, пропусков нет) - количество спален, расположенных выше цокольного этажа;
* ***KitchenAbvGr*** (тип данных *int64*, пропусков нет) - количество кухонь;
* ***KitchenQual*** (тип данных *object*, пропусков нет) - качество кухни;
* ***TotRmsAbvGrd*** (тип данных *int64*, пропусков нет) - общее количество комнат над уровнем земли (без учета ванных комнат);
* ***Functional*** (тип данных *object*, пропусков нет) - оценка функциональности дома;
* ***Fireplaces*** (тип данных *int64*, пропусков нет) - количество каминов;
* ***FireplaceQu*** (тип данных *object*, **690** пропусков) - качество камина;
* ***GarageType*** (тип данных *object*, **81** пропуск) - местоположение гаража;
* ***GarageYrBlt*** (тип данных *float64*, **81** пропуск) - год постройки гаража;
* ***GarageFinish*** (тип данных *object*, **81** пропуск) - внутренняя отделка гаража;
* ***GarageCars*** (тип данных *int64*, пропусков нет) - размер гаража вместимостью автомобилей;
* ***GarageArea*** (тип данных *int64*, пропусков нет) - размер гаража в квадратных футах;
* ***GarageQual*** (тип данных *object*, **81** пропуск) - качество гаража;
* ***GarageCond*** (тип данных *object*, **81** пропуск) - состояние гаража;
* ***PavedDrive*** (тип данных *object*, пропусков нет) - мощеная подъездная дорога;
* ***WoodDeckSF*** (тип данных *int64*, пропусков нет) - площадь деревянной террасы в квадратных футах;
* ***OpenPorchSF*** (тип данных *int64*, пропусков нет) - площадь открытой веранды в квадратных футах;
* ***EnclosedPorch*** (тип данных *int64*, пропусков нет) - площадь закрытой веранды в квадратных футах;
* ***3SsnPorch*** (тип данных *int64*, пропусков нет) - площадь веранды для использования в течение трех сезонов (в квадратных футах);
* ***ScreenPorch*** (тип данных *int64*, пропусков нет) - площадь застекленной веранды в квадратных футах;
* ***PoolArea*** (тип данных *int64*, пропусков нет) - площадь бассейна в квадратных футах;
* ***PoolQC*** (тип данных *object*, **1453** пропуска) -  качество бассейнов;
* ***Fence*** (тип данных *object*, **1179** пропусков) - качество забора;
* ***MiscFeature*** (тип данных *object*, **1406** пропусков) - дополнительные функции, не охваченные другими категориями;
* ***MiscVal*** (тип данных *int64*, пропусков нет) - ценность дополнительных функций;
* ***MoSold*** (тип данных *int64*, пропусков нет) - месяц продажи;
* ***YrSold*** (тип данных *int64*, пропусков нет) - год продажи;
* ***SaleType*** (тип данных *object*, пропусков нет) - тип продажи;
* ***SaleCondition*** (тип данных *object*, пропусков нет) - условия продажи;
* ***SalePrice*** (тип данных *int64*, пропусков нет) - цена продажи в $.

> Итого: **3** типа данных __*float64*__, **34** типа данных __*int64*__, **43** типа данных __*object*__

Далее посмотрим на распределение цен на жилье.

![plot6](/Task2/images/plot6.svg)

По нему видно, что большая часть домов оценивается в районе \$145 000 - \$150 000, основую часть составляют дома \$75 000 - \$300 000.

Далее рассмотрим распределение числовых показателей датасета.

Для этого составим список типов данных, а затем на его основании построим датафрейм только с числовыми колонками.

```
list(set(dataset_df.dtypes.tolist()))

df_num = dataset_df.select_dtypes(include = ['float64', 'int64'])
```

Построим гистограммы распределения для каждого параметра.

![plot7-43](/Task2/images/plot7-43.svg)

Из графиков следует, что большая часть построек имеют (имели):
* длину прилегающей улицы **до 100 футов**;
* размер участка **до 25 000 футов квадратных**;
* качество отделки на **5-8** баллов из **10**;
* оценку в **5** баллов из **10**;
* дату постройки **после 2000-го года**;
* реновацию **в 1950 г** или **в 2000 г и позже**;
* площадь облицовки **менее 50 футов**;
* общую площадь подвального помещения **от 50 до 2500 футов квадратных**;
* площадь первого этажа **от 300 до 750 футов квадратных**;
* площадь площадь жилого пространства над уровнем земли **от 800 до 2000 футов квадратных**;
* **одну полноценную ванную** в подвале или не имеют вовсе;
* **1 или 2 полноценные ванны** в помещениях над уровнем земли;
* **1 туалет** в помещениях над уровнем земли;
* **2-4 спальни** выше цокольного этажа;
* **1 кухню**;
* **4-8 комнат**;
* **1-2 камина**;
* дату постройки гаража **после 2000-го года**;
* места в гараже на **1-3 машины**;
* площадь гаража **до 1000 футов квадратных**;
* сезон продажи - **лето**.

### Подготовка данных

Необходимость в заполнении пропусков и фильтрации колонок **отсутствует**, так как TF-DF делает это сам. Напишем функцию для разделения выборки на *train* и *val* в соотношении 70/30:
```
def split_dataset(dataset, test_ratio=0.30):
  test_indices = np.random.rand(len(dataset)) < test_ratio
  return dataset[~test_indices], dataset[test_indices]

train_ds_pd, valid_ds_pd = split_dataset(dataset_df)
print("{} examples in training, {} examples in testing.".format(
    len(train_ds_pd), len(valid_ds_pd)))
```

Перед тем, как начать обучать модель, необходимо переконвертировать датафрейм из формата pandas (pd) в формат TensorFlow Datasets (tf), что позволит ускорить обучение.

### Создание и обучение модели случайного леса

По умолчанию модель Random Forest настроена на обучение задаче классификации. Так как у нас регрессионная задача, передадим параметром ```tfdf.keras.Task.REGRESSION```:

```
label = 'SalePrice'
train_ds = tfdf.keras.pd_dataframe_to_tf_dataset(train_ds_pd, label=label, task = tfdf.keras.Task.REGRESSION)
valid_ds = tfdf.keras.pd_dataframe_to_tf_dataset(valid_ds_pd, label=label, task = tfdf.keras.Task.REGRESSION)
```

Создадим модель Random Forests:
```
rf = tfdf.keras.RandomForestModel(task = tfdf.keras.Task.REGRESSION)
rf.compile(metrics=["mse"]) # Optional, you can use this to include a list of eval metrics
```

Далее обучим модель:
```
rf.fit(x=train_ds)
```

```
...
...
...
Model trained in 0:00:02.141434
Compiling model...
Model compiled.
```

### Результаты обучения

На графике представлено, как изменяется RMSE с увеличением числа деревьев.

![plot44](/Task2/images/plot44.svg)]

На графике зависимости RMSE от числа деревьев видно, что ошибка быстро снижается при небольшом числе деревьев и затем стабилизируется, что свидетельствует о том, что усреднение предсказаний в ансамбле достигло оптимального уровня. Таким образом, выбранное количество деревьев в лесу является обоснованным, а дальнейшее увеличение числа деревьев не приводит к значительному улучшению качества модели.

Также мы можем посмотреть основные показатели OOB-датасета:

`inspector = rf.make_inspector()`
`inspector.evaluation()`

Результат:

`Evaluation(num_examples=1025, accuracy=None, loss=None, rmse=29623.313095528443, ndcg=None, aucs=None, auuc=None, qini=None)`

То есть:
* число объектов в OOB-выборке равно ***1025***;
* среднеквадратичная ошибка модели RMSE на OOB-объектах равна ***$29623.31***. 

Далее оценим модель на валидационном множестве:

```
evaluation = rf.evaluate(x=valid_ds,return_dict=True)

for name, value in evaluation.items():
  print(f"{name}: {value:.4f}")
```
Результат:
``` 
1/1 [==============================] - 1s 644ms/step - loss: 0.0000e+00 - mse: 971344704.0000
loss: 0.0000
mse: 971344704.0000
```

Здесь среднеквадратичная ошибка MSE на валидационном множестве равна *971344704*. Сравним ошибки модели на OOB и на валидационном множестве. Для этого переведем RMSE, полученное ранее, в MSE: $RMSE = \sqrt{MSE} = \sqrt{971344704}≈\$31166.40$. 

Близость полученных ошибок свидетельствует о том, что модель хорошо сообщается, нет сильного переобучения и модель устойчива к изменениям данных.

Далее проанализируем относительные важности признаков, рассчитанные на основе влияния каждого признака на уменьшение ошибки при разбиении дерева. Рассмотрим, какие важности нам доступны:

```
print(f"Available variable importances:")
for importance in inspector.variable_importances().keys():
  print("\t", importance)
```

```
Available variable importances:
	 INV_MEAN_MIN_DEPTH
	 SUM_SCORE
	 NUM_AS_ROOT
	 NUM_NODES
```
Здесь:
* `INV_MEAN_MIN_DEPTH` (инверсия средней минимальной глубины) - показывает, **насколько близко к корню дерева** признак используется для разбиения;
* `NUM_AS_ROOT` - число разбиений, где признак стоит в корне дерева;
* `NUM_NODES` - общее число разбиений (узлов) в деревьях, где признак использовался;
* `SUM_SCORE` - cуммарный вклад признака во все разбиения.

Высокие значения этих показателей указывают на важность признака для модели, тогда как низкие значения свидетельствуют о его незначительном влиянии на предсказание.

Выведем признаки по  `NUM_AS_ROOT`:

`inspector.variable_importances()["NUM_AS_ROOT"]`

Результат:
```
[("OverallQual" (1; #62), 123.0),
 ("Neighborhood" (4; #59), 44.0),
 ("ExterQual" (4; #22), 43.0),
 ("GarageCars" (1; #32), 32.0),
 ("GrLivArea" (1; #38), 30.0),
 ("YearBuilt" (1; #76), 11.0),
 ("KitchenQual" (4; #44), 7.0),
 ("GarageArea" (1; #31), 3.0),
 ("TotalBsmtSF" (1; #73), 3.0),
 ("BsmtQual" (4; #14), 2.0),
 ("FullBath" (1; #29), 1.0),
 ("GarageYrBlt" (1; #37), 1.0)]
```

Визуализируем это с помощью столбчатой диаграммы:

![plot45](/Task2/images/plot45.svg)

Наконец, предскажем с помощью нашей модели стоимости домов из датасета [test.csv](https://www.kaggle.com/competitions/house-prices-advanced-regression-techniques/data?select=test.csv).
```
test_file_path = "/kaggle/input/competitions/house-prices-advanced-regression-techniques/test.csv"
test_data = pd.read_csv(test_file_path)
ids = test_data.pop('Id')

test_ds = tfdf.keras.pd_dataframe_to_tf_dataset(
    test_data,
    task = tfdf.keras.Task.REGRESSION)

preds = rf.predict(test_ds)
output = pd.DataFrame({'Id': ids,
                       'SalePrice': preds.squeeze()})

output.head()
```

Приведем здесь первые 5 цен:
* Id = 1461, SalePrice = 125481.906250;
* Id = 1462, SalePrice = 152300.203125;
* Id = 1463, SalePrice = 181514.296875
* Id = 1464, SalePrice = 184205.734375
* Id = 1465, SalePrice = 193713.515625.

## Вывод

В рамках решения задачи предсказания цен домов на наборе данных **House Prices** из соревнования Kaggle был реализован и проанализирован **алгоритм случайного леса** на базе библиотеки **TensorFlow Decision Forests (TF-DF)**. Модель демонстрирует высокую эффективность и устойчивость благодаря ансамблевому подходу, сочетающему бэггинг и случайный выбор признаков на каждом шаге разбиения дерева.

### Основные результаты

1. **Качество модели**:
	* ***OOB‑RMSE ≈ $31166.40** — среднеквадратичная ошибка на внутренней оценочной выборке (out‑of‑bag объекты),    
	* ***Validation RMSE ≈ $29623.31*** — ошибка на независимом валидационном множестве (MSE ≈ *971344704*).
	
    Близость OOB‑ и валидационной ошибок свидетельствует об **отсутствии сильного переобучения** и **стабильной обобщающей способности** модели. Разброс ошибок при повторных прогонах остаётся умеренным, что подтверждает **устойчивость** алгоритма к случайным изменениям в бутстрап‑выборках.
    
2. **Анализ поведения ансамбля**:  
    График зависимости OOB‑RMSE от числа деревьев демонстрирует **быстрое снижение ошибки** при малом числе деревьев с последующей **стабилизацией**, что указывает на оптимальность выбранного числа деревьев и эффективность усреднения предсказаний.
    
3. **Важность признаков**:  
    Анализ переменных важностей (`INV_MEAN_MIN_DEPTH`, `NUM_AS_ROOT`, `NUM_NODES`, `SUM_SCORE`) выявил ключевые факторы, определяющие цену дома: **OverallQual** (общее качество), **ExterQual** (качество внешней отделки), **Neighborhood** (район), **GarageCars** (число машин, помещающихся в гараже), **GrLivArea** (жилая площадь). Эти признаки вносят наибольший вклад в уменьшение ошибки при разбиении деревьев, в то время как менее значимые признаки (например, год продажи) оказывают минимальное влияние на прогнозы.

### Теоретические преимущества случайного леса

Алгоритм эффективно устраняет **основные недостатки** одиночных деревьев решений:

- **Высокая дисперсия** снижается за счёт усреднения множества деревьев, обученных на разных бутстрап‑выборках,

- **Корреляция между деревьями** уменьшается благодаря случайному выбору подмножества признаков на каждом узле разбиения,

- **OOB‑оценка** обеспечивает **внутренний контроль качества** без необходимости явного разделения данных на валидационные множества.

## Практическая применимость

Разработанная модель обладает **высокой интерпретируемостью** (визуализация деревьев, важности признаков) и **устойчивостью к пропускам** данных, что делает её подходящей для реальных задач прогнозирования цен недвижимости. Ошибка RMSE ≈ 29–31 тыс. долларов при средних ценах домов порядка 180 тыс. долларов демонстрирует **достаточную точность**для практического применения.